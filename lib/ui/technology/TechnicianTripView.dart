import 'package:flutter/material.dart';

class TechnicianTripView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("旅行"),
    );
  }
}
