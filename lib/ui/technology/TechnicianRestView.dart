import 'package:flutter/material.dart';

class TechnicianRestView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("休息"),
    );
  }
}
